# this module is used for show patern


def right_angle(num_rows):
    for i in range(num_rows):
        for j in range(i):
            print("*",end = " ")

        print()


def squar_pattern(num_rows):
    for i in range(num_rows):
        for j in range(num_rows):
            print("*",end = " ")

        print()























